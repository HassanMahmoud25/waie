import type { MindMap } from "@/types/mind-map";

/**
 * Mind maps are an analytical summary of an episode's content — again,
 * something that requires watching/outlining each episode, not derivable
 * from channel metadata. Left empty for the real import; the "خريطة
 * الحلقة" tab's empty state covers this until real mind maps are authored.
 */
export const mindMaps: MindMap[] = [];
